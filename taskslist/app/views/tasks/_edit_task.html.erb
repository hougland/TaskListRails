<%= form_for @task, :method => :put, :html => {:class => "form-inline"} do |f| %>
  <fieldset>
    <legend>Do it Here</legend>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.submit class: "btn btn-default"%>
  </fieldset>
<% end %>
